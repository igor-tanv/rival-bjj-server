<!DOCTYPE html>
<html>

<head>

</head>

<body>
    <div>
        <div style="margin:100px;">
            <div class="jumbotron" style="padding:40px;">
                <form action="/contract/status/{{contract._id}}" method="POST">
                <h1>Confirmed Match</h1>
                <br>
                    <img src='/avatar-pictures/{{ contract.opponent.avatar }}' width="150" height="150" />
                    <h3>Opponent Name: {{ contract.opponent.firstName }} {{ contract.opponent.lastName }}</h3>
                    <h3>Challenge Status: {{contract.status}}</h3>
                    <h3>Location: {{ contract.school }} </h3>
                    <h3>Date: {{ contract.date.month }} {{ contract.date.day }}, {{ contract.date.year }} at
                        {{ contract.date.hour }}:{{ contract.date.minutes }} </h3>
                    <h3>Rules: {{ contract.rules }} </h3>
                    <h3>Referee: {{ contract.refereeFirstName }} {{ contract.refereeLastName }} </h3>
                    <h3>Comments: {{ contract.comments }} </h3>
                    <br>
                    <p>*You must bring a hard copy of the contract to the match</p>
                    <p><a class="btn btn-primary btn-lg" href="/contract-get-pdf/{{contract._id}}" role="button">Print Contract</a></p>
                </form>
                <br>
            </div>

        </div>
    </div>
    </div>
</body>

</html>